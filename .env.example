# Example env for local/dev
# For local development set:
# TRAEFIK_API_INSECURE=true
# TRAEFIK_DASHBOARD_BIND=0.0.0.0
# TRACCAR_BIND_HOST=0.0.0.0
# TRACCAR_DOMAIN=traccar.local

# For production set:
# TRAEFIK_API_INSECURE=false
# TRAEFIK_DASHBOARD_BIND=127.0.0.1
# TRACCAR_BIND_HOST=127.0.0.1
# TRACCAR_DOMAIN=yourdomain.com

TRACCAR_DB_USER=traccar
TRACCAR_DB_PASS=traccar
MYSQL_ROOT_PASSWORD=rootpassword
TRACCAR_DOMAIN=traccar.local
TRAEFIK_API_INSECURE=true
TRAEFIK_DASHBOARD_BIND=0.0.0.0
TRACCAR_BIND_HOST=0.0.0.0
TRACCAR_ENTRYPOINTS=web
TRACCAR_TLS=false
CERT_RESOLVER=le
TZ=UTC
MYSQL_DATA_DIR=./mysql/mysql-data
TRACCAR_DATA_DIR=./traccar/data
TRACCAR_LOG_DIR=./traccar/logs

# Contact email for Let's Encrypt (used by Traefik via LE_EMAIL). Set this in `.env.prod` for production.
LE_EMAIL=you@example.com

# To run locally with the Traefik dashboard enabled:
#  - Set `TRAEFIK_API_INSECURE=true` and `TRAEFIK_DASHBOARD_BIND=0.0.0.0` in your `.env` (or export the env vars), then run `docker compose up -d`.
#  - For production, keep `TRAEFIK_API_INSECURE=false` and `TRAEFIK_DASHBOARD_BIND=127.0.0.1` (binds dashboard to localhost only).
